export type UserAccountDetailsSchema = {
    FirstName: string;
    LastName: string;
    CreatedTime: Date;
    Email: string;
    PhoneNumber: string;
}

export type UserChangePassswordSchema = {
    message: string;
}
